public class Parser
{
	private final static String newPara = "\u0x0a" // TODO - find actual utf 16 for paragraph break
	private static tokenise(String toTokenise) throws BadSyntaxException
	{
		String consume = toTokenise;
		int pos = 0;
		while(pos < consume.length)
		{

		}
	}
}
